I'll analyze the current frontend structure and add authentication features without modifying existing functionality.

I'll analyze the current frontend structure and add authentication features without modifying existing functionality.

I'll now read the existing Vue components to understand the current structure before adding authentication features.

I'll now create a plan and implement the authentication features for this Vue 3 frontend without modifying existing functionality.

I'll create the authentication features for your Vue 3 frontend. Let me start by creating the necessary utilities and components.

I'll now add Vue Router and update the necessary files to enable navigation between pages.

I'll try installing vue-router with a different registry.

I'll now create the router configuration and update the necessary files.

I'll now update the main App.vue and main.js to integrate the router, and add the authentication styles.

I'll now add the authentication styles to complete the implementation.

## 认证功能实现完成

已成功为 Vue 3 前端添加完整的用户认证功能，**完全不影响现有 Research 功能**。

---

### 📁 新增文件结构

#### **工具类**
- `@g:\llm_swift\deepresearch\frontend\src\utils\auth.js:1-62` - Token 管理和 API 请求封装

#### **页面组件**
- `@g:\llm_swift\deepresearch\frontend\src\views\Login.vue:1-82` - 登录页面
- `@g:\llm_swift\deepresearch\frontend\src\views\Register.vue:1-89` - 注册页面
- `@g:\llm_swift\deepresearch\frontend\src\views\Research.vue:1-169` - Research 主页面（从原 App.vue 迁移）

#### **UI 组件**
- `@g:\llm_swift\deepresearch\frontend\src\components\UserMenu.vue:1-123` - 用户菜单（右上角）

#### **路由配置**
- `@g:\llm_swift\deepresearch\frontend\src\router\index.js:1-27` - Vue Router 配置

---

### 🔧 修改的文件

#### **核心文件**
- `@g:\llm_swift\deepresearch\frontend\src\App.vue:1-10` - 简化为路由容器
- `@g:\llm_swift\deepresearch\frontend\src\main.js:1-9` - 集成 Vue Router
- `@g:\llm_swift\deepresearch\frontend\src\style.css:78-199` - 新增认证页面样式

---

### ✅ 功能特性

#### **1. 登录页面** (`/login`)
- 用户名 + 密码登录
- 调用 `POST /api/auth/login`
- 成功后保存 token 到 localStorage
- 自动跳转到主页

#### **2. 注册页面** (`/register`)
- 用户名 + 邮箱 + 密码
- 调用 `POST /api/auth/register`
- 成功后跳转到登录页

#### **3. 用户菜单**（右上角）
- **未登录**：显示「登录 / 注册」按钮
- **已登录**：显示用户名 + 下拉菜单
  - 用户信息（只读）
  - 返回 Research 页面
  - 退出登录

#### **4. Token 管理**
- 登录后自动保存到 `localStorage`
- 后续请求可通过 [apiRequest()](cci:1://file:///g:/llm_swift/deepresearch/frontend/src/utils/auth.js:39:0-63:1) 自动携带 `Authorization: Bearer <token>`
- 401 响应自动清除 token 并跳转登录

---

### 🎯 设计原则

✅ **零侵入** - 原有 Research / SSE 逻辑完全保留在 [Research.vue](cci:7://file:///g:/llm_swift/deepresearch/frontend/src/views/Research.vue:0:0-0:0)  
✅ **最小改动** - 仅修改 App.vue 为路由容器  
✅ **风格一致** - 沿用现有 CSS 变量和设计语言  
✅ **前端透明** - 不解析 token，不判断角色权限

---

### 🚀 启动测试

```bash
cd g:\llm_swift\deepresearch\frontend
npm run dev
```

访问：
- `http://localhost:5173/` - Research 主页
- `http://localhost:5173/login` - 登录页
- `http://localhost:5173/register` - 注册页